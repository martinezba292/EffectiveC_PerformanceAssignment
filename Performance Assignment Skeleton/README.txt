The program apply some operations to the images placed in the input folder
and saves them in the output folder.
The operations applied are grayscale, brighten pixels, 90 degrees rotation and scale image size by 2.